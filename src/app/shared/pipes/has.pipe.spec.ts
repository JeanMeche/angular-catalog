/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HasPipe } from './has.pipe';

describe('Pipe: Hase', () => {
  it('create an instance', () => {
    let pipe = new HasPipe();
    expect(pipe).toBeTruthy();
  });
});
